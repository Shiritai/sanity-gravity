name: Reusable Test

on:
  workflow_call:
    inputs:
      arch:
        required: true
        type: string
      repo_lc:
        required: true
        type: string
      tag:
        required: true
        type: string
      tags:
        required: true
        type: string
        description: "JSON array of final tags to pull for testing"

jobs:
  test:
    name: Integration Tests (${{ inputs.arch }})
    runs-on: ${{ inputs.arch == 'arm64' && 'ubuntu-24.04-arm' || 'ubuntu-latest' }}
    permissions:
      contents: read
      packages: read

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Pull all final images
        run: |
          REPO_LC="${{ inputs.repo_lc }}"
          TAG_VAL="${{ inputs.tag }}"
          ARCH_VAL="${{ inputs.arch }}"

          for image_tag in $(echo '${{ inputs.tags }}' | jq -r '.[]'); do
            echo "Pulling ${image_tag}..."
            docker pull ghcr.io/${REPO_LC}-${image_tag}:${TAG_VAL}-${ARCH_VAL}
            docker tag ghcr.io/${REPO_LC}-${image_tag}:${TAG_VAL}-${ARCH_VAL} sanity-gravity:${image_tag}
          done

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.10'

      - name: Install Test Dependencies
        run: |
          sudo apt-get update && sudo apt-get install -y socat
          python -m pip install --upgrade pip
          pip install pytest requests urllib3 PyYAML pytest-timeout

      - name: Run Integration Tests
        run: |
          chmod +x ./sanity-cli
          ./sanity-cli test
