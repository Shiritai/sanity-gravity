services:
  ag-xfce-vnc:
    image: ${SANITY_IMAGE_AG_XFCE_VNC:-sanity-gravity:ag-xfce-vnc}
    command:
    - /usr/bin/supervisord
    - -c
    - /etc/supervisor/supervisord.conf
    environment:
    - HOST_UID=${HOST_UID:-1000}
    - HOST_GID=${HOST_GID:-1000}
    - HOST_USER=${HOST_USER:-developer}
    - HOST_PASSWORD=${HOST_PASSWORD:-antigravity}
    - VNC_PW=${VNC_PW:-${HOST_PASSWORD}}
    - VNC_RESOLUTION=${VNC_RESOLUTION:-1280x720}
    - VNC_DEPTH=${VNC_DEPTH:-24}
    volumes:
    - sg_ag-xfce-vnc:/home/${HOST_USER:-developer}
    - ${WORKSPACE_DIR:-./workspace}:/home/${HOST_USER:-developer}/workspace
    ports:
    - ${VNC_PORT:-5901}:5901
    - ${NOVNC_PORT:-6901}:6901
    - ${SSH_HOST_PORT:-2222}:22
    network_mode: bridge
    shm_size: 512m
    restart: unless-stopped
    stop_grace_period: 30s
    cap_drop:
    - ALL
    cap_add:
    - CHOWN
    - DAC_OVERRIDE
    - FOWNER
    - KILL
    - SETGID
    - SETUID
    - NET_BIND_SERVICE
    - SYS_CHROOT
    - AUDIT_WRITE
    pids_limit: 2048
    ulimits:
      nofile:
        soft: 65536
        hard: 65536
      core:
        soft: 0
        hard: 0
    labels:
      sanity.gravity.managed: 'true'
      sanity.gravity.home-volume: 'true'
volumes:
  sg_ag-xfce-vnc:
    name: sg-${COMPOSE_PROJECT_NAME:-sanity-gravity}-ag-xfce-vnc
